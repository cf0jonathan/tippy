<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Tippy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Cool color palette (blues & teals) -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorPrimaryContainer">@color/primaryContainer</item> <!-- light blue container -->
        <item name="colorOnPrimaryContainer">@color/onPrimaryContainer</item>
        <item name="colorSecondary">@color/secondary</item> <!-- teal accent -->
        <item name="colorOnSecondary">@color/onSecondary</item>
        <item name="android:colorBackground">@color/background</item> <!-- very light blue background -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorError">@color/error</item>
        <item name="android:textColorPrimary">@color/onBackground</item> <!-- deep navy text on background -->
        <item name="colorOnSurface">@color/onSurface</item>
        <item name="colorSurfaceVariant">@color/surfaceVariant</item> <!-- subtle surface variant -->
        <item name="colorOutline">@color/outline</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Tippy" parent="Base.Theme.Tippy" />
</resources>